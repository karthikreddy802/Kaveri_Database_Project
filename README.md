
# Kaveri Stays — Hotel Management System

An enterprise-grade Hotel Management System built around an existing PostgreSQL schema. Integrates Django (admin and ORM), FastAPI (RESTful API), and React (Vite-based modern UI).

---

## 🛠️ Stack & Architecture

- **Backend**: FastAPI + Django ORM + PostgreSQL (`psycopg` v3)
- **Frontend**: React + Vite + Vanilla CSS (Dark glassmorphism theme)
- **Routing**: Dev Proxy via Vite to FastAPI backend
- **Authentication**: JWT token pair (15m access, 7d refresh) with token rotation security

---

## 🚀 Setup & Launch Instructions

### 1. Environment Configuration

Create a `.env` file in the project root:

```env
SECRET_KEY=9e82c161101e4004bc228c25785a9c049615a017
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7
DB_NAME=kaveri
DB_USER=postgres
DB_PASSWORD=2004
DB_HOST=localhost
DB_PORT=5432
```

### 2. Backend Setup
From the `Backend/Kaveri` directory:

```bash
# Verify psycopg version and environment
python -m pip install -r requirements.txt

# Run seed accounts script to generate credentials
python seed_accounts.py

# Launch the FastAPI application
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 3. Frontend Setup
From the `Frontend` directory:

```bash
# Install dependencies
npm install

# Run the development server (which proxies requests to backend:8000)
npm run dev
```

---

## 🔐 Credentials generated by `seed_accounts.py`

### 👑 Owner Account
- **Email**: `owner@kaveri.com`
- **Password**: `KaveriOwner@2025`

### 💼 Managers (Scoped to respective properties)
- **Property 1 Manager**: `manager1@kaveri.com` / `Manager1@2025`
- **Property 2 Manager**: `manager2@kaveri.com` / `Manager2@2025`
- **Property 3 Manager**: `manager3@kaveri.com` / `Manager3@2025`

### ⚙️ Staff Account
- **Property 1 Staff**: `staff@kaveri.com` / `Staff@2025`

### 👥 Guest Accounts
- Email: Any guest email from `guest` table (e.g., `aarav.sharma@example.com`)
- Password: `Kaveri@2025`


