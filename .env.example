# Kaveri Stays Environment Configuration Template
# Copy this file to .env and fill in the values before starting the application

# Security
SECRET_KEY=

# Database Configuration (if different from Django default settings)
DB_NAME=kaveri
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
